namespace SmartTranslator.Infrastructure.TemplateStrings;

/// <summary> Execution environment type </summary>
public enum EnvironmentType
{
    Unknown = 0,

    /// <summary> Environment type doesn't matter </summary>
    Any = 1000
}
